name: DEV DEPLOYMENT

on:
  push:
    branches: ["dev"]

jobs:
  build:
    runs-on:
      - self-hosted
      - dev
    steps:
      - uses: actions/checkout@v3
      - name: Use Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 12.x
      - name: Install Dependencies
        run: yarn
      - name: Build SPA
        run: npx quasar build
      - name: Move Build Files to Directory
        run: cp -R dist/spa /var/www/amt-clone/amt-viol/dist/
